# Global Rules

## Testing

After making any code changes, delegate to the @fix-failing-tests subagent to run the full test suite and fix any failures. Do not consider the task complete until all tests pass.

